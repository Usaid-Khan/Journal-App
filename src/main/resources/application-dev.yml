spring:
  application:
    name: journal-app
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: ${DB_NAME}
      auto-index-creation: true

server:
  port: 8080
  servlet:
    context-path: /journal

weather:
  api:
    key: ${WEATHER_API_KEY}