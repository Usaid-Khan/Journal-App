spring:
  application:
    name: journal-app
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: ${DB_NAME}
      auto-index-creation: true

server:
  port: 8081
  servlet:
    context-path: /journal